import React from 'react'
import ReactDOM from 'react-dom'

import App from '@no-code/App'

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('react-root')
)
